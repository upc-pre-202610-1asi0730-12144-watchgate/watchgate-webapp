services:
  watchgate-webapp:
    image: watchgate-webapp:1.0.0
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080/api/v1}
    ports:
      - "8081:80"
